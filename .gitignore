# Secrets — never commit (state.json IS tracked: it holds only public video
# IDs + timestamps and is committed by CI as the sync cursor).
client_secrets.json
token.json

# Built binary
/youtube-updater

# Go
/vendor/

# Codegraph index (generated by `codegraph init`)
.codegraph/
